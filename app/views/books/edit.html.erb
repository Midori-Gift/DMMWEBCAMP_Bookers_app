<div class="editBook">
    <header>
      <h2>Editing Book</h2>
        <% if @book.errors.any? %>
            <%= @book.errors.count %>errors prohibited this book from being saved:た
            <% @book.errors.full_messages.each do |message| %>
              <%= message %>
            <% end %>
          <% end %>

    </header>
    <%= form_with model: @book, local:true do |f| %>
      <div class="title-box">
        <label>Title</label>
        <%= f.text_field :title %>
      </div>
      <div class="body-box">
        <label>Body</label>
      <%= f.text_area :body %>
      </div>
      <div class="update-buttun">
        <%= f.submit 'Update Book' %>
      </div>
      <% end %>
         <div class="edit-buttun">
            <%= link_to "Show", book_path(@book.id) %>
            <%= link_to "Back", books_path %>
        </div>
    </div>